<Window x:Class="Hourglass.Views.GamePickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:Hourglass.Views"
        xmlns:converters="clr-namespace:Hourglass.Converters"
        Style="{StaticResource AppWindow}"
        Title="Добавить игры"
        Width="640" Height="600"
        MinWidth="560" MinHeight="440"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility" />
        <converters:InverseBooleanConverter x:Key="InverseBool" />
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Добавить игры" Style="{StaticResource TitleText}" />
            <TextBlock Style="{StaticResource SecondaryText}" Margin="0,2,0,0">
                <Run Text="Аккаунт" />
                <Run Text="{Binding AccountName, Mode=OneWay}" FontWeight="SemiBold" />
            </TextBlock>
        </StackPanel>

        <!-- Search + refresh -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Tag="Поиск по названию или AppID" />
            <Button Grid.Column="1"
                    Content="Обновить из Steam"
                    Style="{StaticResource SecondaryButton}"
                    Command="{Binding RefreshCommand}"
                    Margin="8,0,0,0"
                    ToolTip="Перечитать список игр — например после покупки. Аккаунт должен быть запущен." />
        </Grid>

        <!-- Status strip -->
        <Border Grid.Row="2"
                Background="{StaticResource WarningSoftBrush}"
                CornerRadius="6"
                Padding="12,8"
                Margin="0,0,0,12"
                Visibility="{Binding HasStatusMessage, Converter={StaticResource BoolToVisibility}}">
            <TextBlock Text="{Binding StatusMessage}"
                       Foreground="{StaticResource WarningBrush}"
                       TextWrapping="Wrap" />
        </Border>

        <!-- List -->
        <Border Grid.Row="3" Style="{StaticResource InsetCard}" Padding="6">
            <Grid>
                <ListBox ItemsSource="{Binding Items}"
                         ItemContainerStyle="{StaticResource PlainListItem}"
                         VirtualizingPanel.IsVirtualizing="True"
                         VirtualizingPanel.VirtualizationMode="Recycling"
                         ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Background="Transparent" Padding="8,6" Margin="0,1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                              IsEnabled="{Binding IsAlreadyAdded, Converter={StaticResource InverseBool}}"
                                              VerticalAlignment="Center" />

                                    <Grid Grid.Column="1" Width="92" Height="35" Margin="12,0,0,0">
                                        <Border CornerRadius="4" Background="{StaticResource ElevatedBrush}" />
                                        <TextBlock Text="{Binding AppId}"
                                                   Style="{StaticResource MetricText}"
                                                   Foreground="{StaticResource TextMutedBrush}"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center" />
                                        <Border CornerRadius="4">
                                            <Border.Background>
                                                <ImageBrush Stretch="UniformToFill"
                                                            ImageSource="{Binding Capsule}" />
                                            </Border.Background>
                                        </Border>
                                    </Grid>

                                    <StackPanel Grid.Column="2" Margin="12,0,0,0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}"
                                                   TextTrimming="CharacterEllipsis"
                                                   FontWeight="SemiBold" />
                                        <TextBlock Text="{Binding SubtitleText}"
                                                   Style="{StaticResource MetricText}"
                                                   Margin="0,2,0,0" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"
                            Visibility="{Binding IsRefreshing, Converter={StaticResource BoolToVisibility}}">
                    <views:Spinner HorizontalAlignment="Center" />
                    <TextBlock Text="Спрашиваем Steam…"
                               Style="{StaticResource SecondaryText}"
                               HorizontalAlignment="Center"
                               Margin="0,10,0,0" />
                </StackPanel>

                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"
                            Visibility="{Binding ShowEmptyHint, Converter={StaticResource BoolToVisibility}}">
                    <TextBlock Text="Библиотека пуста"
                               Style="{StaticResource SecondaryText}"
                               HorizontalAlignment="Center" />
                    <TextBlock Text="Добавьте игру по AppID в поле ниже"
                               Style="{StaticResource MetricText}"
                               Foreground="{StaticResource TextMutedBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,4,0,0" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Manual entry -->
        <Border Grid.Row="4" Style="{StaticResource Card}" Padding="12" Margin="0,12,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding ManualAppId, UpdateSourceTrigger=PropertyChanged}"
                         Tag="AppID"
                         MaxLength="9" />
                <TextBox Grid.Column="1"
                         Text="{Binding ManualName, UpdateSourceTrigger=PropertyChanged}"
                         Tag="Название (необязательно)"
                         Margin="8,0,0,0" />
                <Button Grid.Column="2"
                        Content="Добавить"
                        Style="{StaticResource SecondaryButton}"
                        Command="{Binding AddManualCommand}"
                        Margin="8,0,0,0" />
            </Grid>
        </Border>

        <!-- Footer -->
        <Grid Grid.Row="5" Margin="0,16,0,0">
            <TextBlock VerticalAlignment="Center" Style="{StaticResource MetricText}">
                <Run Text="Выбрано:" />
                <Run Text="{Binding SelectedCount, Mode=OneWay}" FontWeight="SemiBold" />
            </TextBlock>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Отмена"
                        Style="{StaticResource GhostButton}"
                        Command="{Binding CancelCommand}"
                        IsCancel="True" />
                <Button Content="Добавить выбранные"
                        Style="{StaticResource PrimaryButton}"
                        Command="{Binding ConfirmCommand}"
                        Margin="8,0,0,0"
                        IsDefault="True" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
