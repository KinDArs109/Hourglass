<Window x:Class="Hourglass.Views.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:Hourglass.Converters"
        Style="{StaticResource AppWindow}"
        Title="Обновление"
        Width="520"
        SizeToContent="Height"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility" />
    </Window.Resources>

    <Border Padding="24">
        <StackPanel>
            <TextBlock Text="{Binding Headline}" Style="{StaticResource TitleText}" />
            <TextBlock Text="{Binding CurrentVersionText}"
                       Style="{StaticResource MetricText}"
                       Margin="0,4,0,16" />

            <Border Style="{StaticResource InsetCard}" Padding="14" MaxHeight="240">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock Text="{Binding Notes}"
                               Style="{StaticResource SecondaryText}"
                               TextWrapping="Wrap"
                               TextTrimming="None" />
                </ScrollViewer>
            </Border>

            <StackPanel Margin="0,16,0,0"
                        Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}">
                <ProgressBar Style="{StaticResource ThinProgress}"
                             Value="{Binding Progress, Mode=OneWay}" />
            </StackPanel>

            <Border CornerRadius="6"
                    Padding="12,8"
                    Margin="0,14,0,0"
                    Visibility="{Binding HasStatusMessage, Converter={StaticResource BoolToVisibility}}">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="{StaticResource SuccessSoftBrush}" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsFailed}" Value="True">
                                <Setter Property="Background" Value="{StaticResource DangerSoftBrush}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource SecondaryText}">
                            <Setter Property="Foreground" Value="{StaticResource SuccessBrush}" />
                            <Setter Property="TextTrimming" Value="None" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsFailed}" Value="True">
                                    <Setter Property="Foreground" Value="{StaticResource DangerBrush}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Border>

            <Grid Margin="0,20,0,0">
                <TextBlock Text="{Binding SizeText}"
                           Style="{StaticResource MetricText}"
                           Foreground="{StaticResource TextMutedBrush}"
                           VerticalAlignment="Center" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Позже"
                            Style="{StaticResource GhostButton}"
                            MinWidth="100"
                            Command="{Binding LaterCommand}"
                            IsCancel="True" />
                    <Button Content="Обновить"
                            Style="{StaticResource PrimaryButton}"
                            MinWidth="120"
                            Margin="8,0,0,0"
                            Command="{Binding InstallCommand}" />
                </StackPanel>
            </Grid>
        </StackPanel>
    </Border>
</Window>
